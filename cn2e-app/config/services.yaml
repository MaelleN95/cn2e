parameters:

services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/'

    App\Security\FormSecurityGuard:
        arguments:
            $defaultLimiter: '@limiter.default'